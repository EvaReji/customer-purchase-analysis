# customer-purchase-analysis
Analyzing customer purchase behavior and product sales trends using Python, SQL, and Excel.
## Day One Progress
- Loaded the Online Retail dataset
- Previewed first rows with `head()`
- Checked dataset structure with `info()`
- Generated summary statistics with `describe()`
- Identified missing values using `isnull().sum()`
- Added markdown explanation headings for clarity
